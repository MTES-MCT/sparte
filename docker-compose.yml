services:
  db:
    image: kartoza/postgis:17-3.5
    shm_size: 1gb
    volumes:
      - db_data:/var/lib/postgresql/data
    env_file: .env
    ports:
      - "5555:5432"
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
    networks:
      - app-network

  s3:
    image: versity/versitygw:latest
    command: ["posix", "/data"]
    environment:
      - ROOT_ACCESS_KEY=localaccess
      - ROOT_SECRET_KEY=localsecret
    ports:
      - "7070:7070"
    volumes:
      - s3_data:/data
    networks:
      - app-network

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    image: sparte/frontend:latest
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3002:3000"
    networks:
      - app-network

  export-server:
    build: ./export
    image: sparte/export-server:latest
    environment:
      - PORT=3000
      - ALLOWED_HOSTS=localhost,django,frontend
    depends_on:
      - frontend
    networks:
      - app-network

  django:
    build: .
    image: sparte/backend:latest
    command: ["sh", "-c", "python manage.py migrate && python manage.py runserver 0.0.0.0:8080 --nothreading"]
    volumes:
      - .:/app
    env_file: .env
    environment:
      - SCALINGO_REDIS_URL=redis://redis:6379/0
      - EXPORT_SERVER_URL=http://export-server:3000
      - EXPORT_BASE_URL=http://django:8080
      - AWS_S3_ENDPOINT_URL=http://s3:7070
      - AWS_S3_REGION_NAME=us-east-1
      - AWS_ACCESS_KEY_ID=localaccess
      - AWS_SECRET_ACCESS_KEY=localsecret
      - AWS_STORAGE_BUCKET_NAME=sparte-local
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
      s3:
        condition: service_started
      export-server:
        condition: service_started
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  db_data:
  s3_data:
